# ---------------------------------------------------------------------------
# RunaxAI — production overrides for runaxai.com
# Deploy with:
#   helm upgrade agenticrag ./helm/agenticrag \
#     -f helm/agenticrag/values.yaml \
#     -f helm/agenticrag/values.prod.yaml \
#     --set secrets.dbAdminPassword=... \
#     --set secrets.dbPassword=... \
#     --set secrets.secretKey=... \
#     --set secrets.minioAccessKey=... \
#     --set secrets.minioSecretKey=... \
#     --set secrets.anthropicApiKey=... \
#     ... (other API keys) \
#     --set certManager.cloudflare.apiToken=... \
#     --set monitoring.grafana.ingress.basicAuth.users='user:$apr1$hash'
# ---------------------------------------------------------------------------

global:
  serviceType: ClusterIP   # ingress handles external traffic in prod

# ---------------------------------------------------------------------------
# Ingress — separate hosts for frontend and API
# ---------------------------------------------------------------------------
ingress:
  enabled: true
  className: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: runaxai.com
      paths:
        - path: /
          pathType: Prefix
          service: frontend
    - host: blog.runaxai.com
      paths:
        - path: /
          pathType: Prefix
          service: frontend
    - host: api.runaxai.com
      paths:
        - path: /
          pathType: Prefix
          service: api
  tls:
    - secretName: runaxai-com-tls
      hosts:
        - runaxai.com
    - secretName: blog-runaxai-com-tls
      hosts:
        - blog.runaxai.com
    - secretName: api-runaxai-com-tls
      hosts:
        - api.runaxai.com

# ---------------------------------------------------------------------------
# MinIO — expose via ingress for presigned URL uploads
# ---------------------------------------------------------------------------
minio:
  ingress:
    enabled: true
    host: storage.runaxai.com
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    tls:
      - secretName: storage-runaxai-com-tls
        hosts:
          - storage.runaxai.com

# ---------------------------------------------------------------------------
# Scaling — 2 replicas of stateless services for zero-downtime deploys and
# crash resilience. Stateful services (postgres/redis/minio) stay at 1.
# HPA stays off until metrics-server is installed.
# Pod anti-affinity is "preferred" (no-op on the 1-node cluster today;
# auto-spreads once a 2nd node is added).
# ---------------------------------------------------------------------------
api:
  replicaCount: 2
  podDisruptionBudget:
    enabled: true
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            topologyKey: kubernetes.io/hostname
            labelSelector:
              matchLabels:
                app.kubernetes.io/name: agenticrag
                app.kubernetes.io/component: api

worker:
  replicaCount: 2
  podDisruptionBudget:
    enabled: true
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            topologyKey: kubernetes.io/hostname
            labelSelector:
              matchLabels:
                app.kubernetes.io/name: agenticrag
                app.kubernetes.io/component: worker

frontend:
  replicaCount: 2
  podDisruptionBudget:
    enabled: true
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            topologyKey: kubernetes.io/hostname
            labelSelector:
              matchLabels:
                app.kubernetes.io/name: agenticrag
                app.kubernetes.io/component: frontend

# ---------------------------------------------------------------------------
# Non-sensitive config — production URLs
# ---------------------------------------------------------------------------
config:
  frontendUrl: "https://runaxai.com"
  apiPublicUrl: "https://api.runaxai.com"
  minioPublicBaseUrl: "https://storage.runaxai.com"
  corsAllowedOrigins: "https://runaxai.com"
  corsAllowLocalhostRegex: "false"
  logLevel: "INFO"
  cookieDomain: "runaxai.com"

# ---------------------------------------------------------------------------
# Cloudflare Tunnel — replaces DDNS + port forwarding
# ---------------------------------------------------------------------------
tunnel:
  enabled: true

# ---------------------------------------------------------------------------
# Cloudflare DDNS — disabled (tunnel handles connectivity)
# ---------------------------------------------------------------------------
ddns:
  enabled: false

# ---------------------------------------------------------------------------
# cert-manager — disabled (Cloudflare Tunnel handles TLS at the edge)
# ---------------------------------------------------------------------------
certManager:
  enabled: false

# ---------------------------------------------------------------------------
# Monitoring — Grafana behind BasicAuth
# ---------------------------------------------------------------------------
monitoring:
  grafana:
    adminUser: "admin"
    adminPassword: ""   # pass via --set monitoring.grafana.adminPassword=...
    ingress:
      enabled: true
      host: grafana.runaxai.com
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
      tls:
        - secretName: grafana-runaxai-com-tls
          hosts:
            - grafana.runaxai.com
      basicAuth:
        enabled: false
